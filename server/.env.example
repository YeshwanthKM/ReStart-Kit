PORT=5001
NODE_ENV=development

# Database connection URL (SQLite - zero setup required)
DATABASE_URL="file:./dev.db"

# JWT Auth secret key
JWT_SECRET="restart_kit_jwt_super_secret_key_change_in_production"
JWT_EXPIRES_IN="7d"
